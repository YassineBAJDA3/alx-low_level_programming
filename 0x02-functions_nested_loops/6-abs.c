#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer.
 *
 * @i: takes int type input fonctoin
 *
 * Return: Always 0 (success)
 *
*/

int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else

	{
		return (i * -1);
	}
}
