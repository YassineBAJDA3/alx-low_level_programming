#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer.
 *
 * @r: takes int type input fonctoin
 *
 * Return: Always 0 .
 *
*/

int _abs(int r)
{
	if (r < 0)
		r = (-1) * r;
		return (0);
}
